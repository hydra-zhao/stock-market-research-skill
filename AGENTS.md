# 多代理工作约定

## 模型与复核链

- 主代理（Sol）负责架构判断、裁决与最终复核。
- 边界清晰、低风险、可独立验证的执行任务，委派给子代理（Luna）。
- 所有 Luna 产出都只是候选结果。Sol 必须检查其证据、修改范围、测试结论
  与 diff，复核通过后才能进入最终提交或作为最终结论。
- 委派时必须明确任务边界、允许修改的文件、禁止触碰的规则、验收命令和
  返回格式。无法清晰划界的任务不得交给 Luna。

## Luna 可承担的任务

- 仓库扫描、文件检索和调用链索引；
- 重复规则检查、静态一致性检查；
- 日志整理、测试执行、测试失败归类；
- 文档同步和 diff 摘要；
- 已由 Sol 明确指定范围与预期结果的机械性小范围修改。

Luna 不得自行确定或修改任何会改变核心结论的规则。发现规则问题时，只能
报告证据、影响范围和候选冲突点；除非 Sol 明确授权机械修改，否则不得编辑。

## Sol 必须亲自负责的事项

以下事项不得委派最终判断，必须由 Sol 亲自分析、裁决并复核：

- 架构判断、复杂调试、跨模块冲突；
- 规则确定性裁决和风险 hard gate；
- 决策层核心规则与买卖边界（生产版）；
- 自我进化准入标准；
- 最终 code review 与是否可提交的决定。

## 核心规则冻结期（生产版纪律，公开版沿用流程形状）

冻结期内允许：确定性校验、测试增强、数据质量修复、已有规则冲突修复、
文档与实现对齐。不得新增核心模块或扩大 hard gate 权限；任何可能改变
核心结论的变更，必须先判定是否属于允许范围，不能证明即停止并报告。

## 验收要求

- Luna 返回结果时必须列出读取或修改的文件、执行的命令、测试结果、
  未解决风险，以及是否触及核心规则。
- Sol 对 Luna 的修改至少执行一次独立 diff 检查，并按风险运行相关测试
  或静态检查。
- 文档对齐不得把尚未实现或尚未获准的行为写成现行规则。

## FINAL-001 · 快速复盘交付契约

- 所有主代理与子代理执行快速复盘时必须调用 `python data/quick_pipeline.py --final`。
- 禁止直接交付 `quick-snapshot-*`、工具 stdout 或 Agent 自行拼装的复盘正文。
- canonical `quick-context-*` 是事实唯一来源；Agent 不得自行选择数字、
  补写持仓或改写 stage/hook 状态。
- 只有 `delivery-manifest-*.json` 中 `publishable=true`，且交付前执行
  `python data/quick_pipeline.py --verify <manifest>` 成功，才允许原样交付
  对应 `quick-review-*` 正文。
- 允许增加一句简短前言；禁止删除 required section、重新组织事实层或引入
  artifact 外事实。
